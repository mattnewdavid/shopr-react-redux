
import Layout from "./components/Layout";

function App() {
  // const isLoggedIn = useSelector(state => state.auth.isLoggedIn)
  
  
  return (
    <div className="App">
      {/* {!isLoggedIn && <Auth />} */}
       <Layout />
    </div>
  );
}

export default App;
